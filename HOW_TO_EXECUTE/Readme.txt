Groupify Algorithm

Streamlit app to create balanced student groups from a CSV.

Steps:
1. Upload CSV (columns: Roll, Name, Email).
2. Enter number of groups.
3. Click "Process Groups".
4. Download ZIP or individual CSVs.

Run:
pip install -r requirements.txt
streamlit run app.py
